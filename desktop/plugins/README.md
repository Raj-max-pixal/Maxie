# MAXie Local Plugins

MAXie keeps the plugin marketplace offline-first for now.

Drop future behavior packs in this folder. A plugin should be a folder with:

- `plugin.json` for name, version, and description
- optional CSS or animation assets
- optional JavaScript behavior loaded only after review

Online publishing, ratings, payments, and remote downloads are intentionally not included yet.
